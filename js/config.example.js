// ======================================================
// 📄 ARQUIVO: config.example.js
// 💡 MODELO DE CONFIGURAÇÃO (sem chaves reais)
//
// Este arquivo serve como exemplo para outros desenvolvedores
// configurarem suas próprias credenciais locais.
//
// 🔒 ATENÇÃO:
// Nunca insira chaves reais neste arquivo — use apenas cópias de exemplo.
// As chaves verdadeiras devem ficar no arquivo "js/config.js" (que está no .gitignore)
// ou definidas em variáveis de ambiente no Firebase Hosting.
// ======================================================

export const CONFIG = {
  // 🔑 Chave de API do Google (substitua pela sua)
  GOOGLE_API_KEY: "SUA_CHAVE_DE_API_AQUI",

  // 📍 ID do local (Place ID) da sua empresa no Google Maps
  PLACE_ID: "SEU_PLACE_ID_AQUI",
};

// ======================================================
// 📘 INSTRUÇÕES DE USO:
//
// 1️⃣ Renomeie este arquivo para "config.js".
// 2️⃣ Substitua os valores de exemplo acima pelas suas credenciais reais.
// 3️⃣ Nunca envie o arquivo "config.js" para o GitHub.
//     Ele está protegido no .gitignore para evitar exposição de chaves.
// ======================================================
